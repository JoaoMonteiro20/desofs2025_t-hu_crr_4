version: '3.9'

services:
  ecoimpact-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7020:7020" # usar porta 7020 interna, 7020 externa
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
